/* const aluno = "Kaleb";
const notaPrimeiroBi = 8; 
const notaSegundoBi = 6.3;
const notaTerceiroBi = 7;
const notaQuartoBi = 6; 

let total = (notaPrimeiroBi + notaSegundoBi + notaTerceiroBi + notaQuartoBi)/4; 

console.log(`A média do aluno no ano foi ${total.toFixed(2)}`);

if (total >= 7){
    total += total * 0.1
    console.log(`Parabéns ${aluno}!! sua média foi ${total.toFixed(2)} e voce passou de ano!!`)
}else {
    console.log(`Infelizmente sua nota foi ${total.toFixed(2)} e não foi aprovado!!`)
}

 */

/* const salarioMensal = 3500; 
const despesasFixas = 1200; 
const despesasVariaveis = 500; 
const economiasMensais = 800;
const bonusAnual = 3000;

const resultado = (salarioMensal - despesasFixas - despesasVariaveis) * 12 + (economiasMensais * 12) + bonusAnual;

console.log(resultado); */

/* let resultado;
resultado = 10 + ((5 * 2) / 3) - 7 + ((15 * 3) / 5) + 20 - (4 * 2);



console.log("O resultado da expressão é:", resultado.toFixed(2)); */

